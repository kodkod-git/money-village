# 주사위 3D 모델 연출 (GLB) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 주사위 눈금 맞추기 미니게임을 GIF+정적 이미지 방식에서, `<model-viewer>`로 렌더링한 실제 3D GLB 모델이 정확한 눈금 방향으로 회전하며 멈추는 연출로 교체한다.

**Architecture:** `index.html`의 `#luckView5`에 기존 `<img id="luckGameImg">`와 나란히 `<model-viewer id="luckDiceModel">`을 추가한다. 주사위 게임일 때만 이 모델을 표시하고, 다른 두 게임(RPS/룰렛)은 지금처럼 `#luckGameImg`를 그대로 쓴다. 회전 중엔 `requestAnimationFrame`으로 모델의 `orientation` 속성을 계속 갱신해 "구르는" 느낌을 내고, 멈출 때는 미리 사람이 육안으로 보정해둔 `_DICE_FACE_ORIENTATION` 테이블의 목표 각도까지 감속 보간(tween)한 뒤 잠시 정지해 결과를 보여준다. 기존 RPS/룰렛 정리 로직(`_luckResetGameImg()`)을 확장해 재사용한다.

**Tech Stack:** `<model-viewer>` (Google 웹 컴포넌트, CDN `<script type="module">`), 순수 JS `requestAnimationFrame` 기반 애니메이션. 빌드 스텝 없음, 기존 프로젝트 관례 그대로.

**참고 문서:** `docs/superpowers/specs/2026-07-09-luck-dice-3d-design.md` (설계 배경 및 결정 사항)

---

## Task 1: `index.html` — model-viewer 스크립트 + `luckDiceModel` 엘리먼트

**Files:**
- Modify: `index.html:30` (CDN 스크립트), `index.html:759` (게임 스테이지 마크업)
- Test: `tests/luck-screen-markup.test.js` (extend)

- [ ] **Step 1: Write the failing test**

`tests/luck-screen-markup.test.js`의 마지막 `console.log(...)` 줄 바로 앞에 추가:

```javascript
// 3D 주사위 모델 (model-viewer)
assert(html.includes('@google/model-viewer'), 'index.html should load the model-viewer web component for the 3D dice');
assert(html.includes('id="luckDiceModel"'), 'play view should have a model-viewer element for the 3D dice');
assert(/<model-viewer[^>]*src="image\/luck\/dice_spin\.glb"/.test(html), 'luckDiceModel should point at the dice_spin.glb asset');
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node tests/luck-screen-markup.test.js`
Expected: FAIL — `index.html should load the model-viewer web component for the 3D dice`

- [ ] **Step 3: index.html 수정**

`index.html` 30번째 줄(`<script src="https://t1.kakaocdn.net/kakao_js_sdk/...`) 바로 다음 줄에 추가:

```html
    <script type="module" src="https://unpkg.com/@google/model-viewer/dist/model-viewer.min.js"></script>
```

`index.html`의 `<img id="luckGameImg" ...>` (758~760번째 줄 부근, `.luck-game-stage` 안)을 다음과 같이 수정:

```html
                <div class="luck-game-stage">
                    <img id="luckGameImg" class="luck-game-img" draggable="false" alt="행운 게임" />
                    <model-viewer id="luckDiceModel" class="luck-game-img" src="image/luck/dice_spin.glb"
                        style="display:none;" disable-zoom></model-viewer>
                </div>
```

(`disable-zoom`만 명시하고 `camera-controls`는 아예 쓰지 않는다 — model-viewer는 `camera-controls` 속성이 존재하기만 하면 값과 무관하게 마우스/터치 궤도 조작이 켜지므로, 프로그램적으로만 회전을 제어하려면 속성 자체를 빼야 한다.)

- [ ] **Step 4: Run test to verify it passes**

Run: `node tests/luck-screen-markup.test.js`
Expected: `luck-screen-markup.test.js (entry + script) passed`

- [ ] **Step 5: Commit**

```bash
git add index.html tests/luck-screen-markup.test.js
git commit -m "feat: load model-viewer and add 3D dice model element to luckScreen"
```

---

## Task 2: `js/luck.js` — 3D 주사위 회전/정지 엔진

**Files:**
- Modify: `js/luck.js` (기존 `_luckDiceStart`/`luckDiceStop` 교체, `_luckResetGameImg` 확장)
- Test: `tests/luck-games.test.js` (dice 관련 assertion 교체)

- [ ] **Step 1: Update the test**

`tests/luck-games.test.js`에서 기존 dice 관련 assertion 중 아래 두 줄을 **삭제**한다:

```javascript
assert(/image\/luck\/dice_spin\.gif/.test(js), 'dice should play a spin GIF while rolling (per the resolved GIF-vs-static-frames design decision)');
```

```javascript
assert(/`image\/luck\/dice_face_\$\{face\}\.png`/.test(js), 'stopping should swap the GIF for a static per-face image so the shown result is always exact — a GIF cannot be paused at an arbitrary frame');
```

나머지 기존 dice assertion(`_luckDiceStart`/`luckDiceStop` 존재, `Math.floor(Math.random() * 6) + 1`, `face === 6`)은 그대로 둔다. 마지막 `console.log('luck-games.test.js passed');` 줄 바로 앞에 다음을 추가한다:

```javascript
// 3D 주사위 (model-viewer)
assert(/const _DICE_FACE_ORIENTATION = \{/.test(js), '_DICE_FACE_ORIENTATION lookup table should exist for the 3D dice calibration');
assert(/model\.orientation/.test(js), 'dice engine should drive the model-viewer via its orientation property');
assert(/requestAnimationFrame\(tumble\)/.test(js), 'dice should tumble continuously via requestAnimationFrame while rolling');
assert(/cancelAnimationFrame\(_diceRollFrame\)/.test(js), 'stopping the dice should cancel the in-flight roll animation frame');
assert(/\}, 800\);/.test(js), 'dice should pause about 0.8s on the settled face before resolving, so the result is actually visible');
assert(/_luckResetGameImg[\s\S]{0,600}_diceRollFrame/.test(js), '_luckResetGameImg should also cancel any in-flight dice animation frame, mirroring the RPS/roulette cleanup fixes');
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node tests/luck-games.test.js`
Expected: FAIL — `_DICE_FACE_ORIENTATION lookup table should exist for the 3D dice calibration`

- [ ] **Step 3: `js/luck.js`의 `_luckResetGameImg()` 확장**

현재 `_luckResetGameImg()`는 다음과 같다:

```javascript
function _luckResetGameImg() {
    if (_luck.rpsCycleTimer) {
        clearInterval(_luck.rpsCycleTimer);
        _luck.rpsCycleTimer = null;
    }
    const img = document.getElementById('luckGameImg');
    if (img) {
        img.classList.remove('luck-roulette-spinning');
        img.style.transform = '';
        img.style.transition = '';
    }
}
```

이를 다음으로 교체한다 (dice 정리 로직 추가 + `img` 표시 상태 복원):

```javascript
function _luckResetGameImg() {
    if (_luck.rpsCycleTimer) {
        clearInterval(_luck.rpsCycleTimer);
        _luck.rpsCycleTimer = null;
    }
    const img = document.getElementById('luckGameImg');
    if (img) {
        img.classList.remove('luck-roulette-spinning');
        img.style.transform = '';
        img.style.transition = '';
        img.style.display = '';
    }
    if (_diceRollFrame) {
        cancelAnimationFrame(_diceRollFrame);
        _diceRollFrame = null;
    }
    const model = document.getElementById('luckDiceModel');
    if (model) model.style.display = 'none';
}
```

(`_diceRollFrame`은 이 task의 Step 4에서 파일 하단에 선언되는 module-level 변수다. `_luckResetGameImg`는 함수가 실제로 *호출*될 때만 이 변수를 참조하므로, 선언보다 앞쪽에서 참조되어도 문제없다 — Task 9~11에서도 같은 방식의 전방 참조가 이미 여러 번 쓰였다.)

- [ ] **Step 4: 기존 dice 섹션을 3D 엔진으로 교체**

`js/luck.js` 파일 끝부분의 다음 블록을 찾는다:

```javascript
// ── 주사위 눈금 맞추기 ────────────────────────────────────────────
// GIF는 프레임 위치를 스크립트로 제어할 수 없어 '멈춤' 클릭 순간
// 정확한 눈금에 멈추게 할 수 없다. 그래서 회전 중엔 연출용 GIF를 재생하고,
// 멈추는 순간 결과에 해당하는 정적 이미지로 즉시 교체해 항상 정확한 결과를 보여준다.
function _luckDiceStart() {
    document.getElementById('luckGameImg').src = 'image/luck/dice_spin.gif';
}

function luckDiceStop() {
    const face = Math.floor(Math.random() * 6) + 1;
    document.getElementById('luckGameImg').src = `image/luck/dice_face_${face}.png`;

    const isWin = face === 6;
    _luckResolve(isWin, { face });
}
```

이 블록 전체를 다음으로 교체한다:

```javascript
// ── 주사위 눈금 맞추기 (3D GLB 모델) ────────────────────────────────
// 각 눈금(1~6)이 카메라를 향할 때의 model-viewer orientation 값.
// GLB 지오메트리만으로는 자동으로 신뢰성 있게 알아낼 수 없어, 실제 파일이
// 도착한 뒤 사람이 육안으로 확인해 채워 넣는다. 아래 값은 채워 넣기 전
// 자리표시자이며, 실제 채워 넣는 작업은 이 plan의 Task 5에서 다룬다.
// (docs/superpowers/specs/2026-07-09-luck-dice-3d-design.md 참고)
const _DICE_FACE_ORIENTATION = {
    1: '0deg 0deg 0deg',
    2: '0deg 0deg 0deg',
    3: '0deg 0deg 0deg',
    4: '0deg 0deg 0deg',
    5: '0deg 0deg 0deg',
    6: '0deg 0deg 0deg',
};

let _diceRollFrame = null;

function _luckParseOrientation(str) {
    return str.split(' ').map(part => parseFloat(part));
}

function _luckLerpOrientation(fromStr, toStr, t) {
    const from = _luckParseOrientation(fromStr);
    const to   = _luckParseOrientation(toStr);
    const out  = from.map((v, i) => v + (to[i] - v) * t);
    return `${out[0]}deg ${out[1]}deg ${out[2]}deg`;
}

function _luckDiceStart() {
    document.getElementById('luckGameImg').style.display = 'none';
    const model = document.getElementById('luckDiceModel');
    model.style.display = 'block';

    let angle = 0;
    function tumble() {
        angle += 6;
        model.orientation = `${angle % 360}deg ${(angle * 1.3) % 360}deg ${(angle * 0.7) % 360}deg`;
        _diceRollFrame = requestAnimationFrame(tumble);
    }
    _diceRollFrame = requestAnimationFrame(tumble);
}

function luckDiceStop() {
    const face = Math.floor(Math.random() * 6) + 1;
    cancelAnimationFrame(_diceRollFrame);
    _diceRollFrame = null;

    const model = document.getElementById('luckDiceModel');
    const startOrientation  = model.orientation || '0deg 0deg 0deg';
    const targetOrientation = _DICE_FACE_ORIENTATION[face];
    const duration  = 600;
    const startTime = performance.now();

    function settle(now) {
        const t     = Math.min(1, (now - startTime) / duration);
        const eased = 1 - Math.pow(1 - t, 3); // ease-out cubic
        model.orientation = t < 1 ? _luckLerpOrientation(startOrientation, targetOrientation, eased) : targetOrientation;
        if (t < 1) {
            _diceRollFrame = requestAnimationFrame(settle);
        } else {
            _diceRollFrame = null;
            setTimeout(() => {
                const isWin = face === 6;
                _luckResolve(isWin, { face });
            }, 800);
        }
    }
    _diceRollFrame = requestAnimationFrame(settle);
}
```

- [ ] **Step 5: Run test to verify it passes**

Run: `node tests/luck-games.test.js`
Expected: `luck-games.test.js passed`

Run: `node tests/luck-core.test.js`
Expected: `luck-core.test.js passed` (회귀 없음 확인)

- [ ] **Step 6: Commit**

```bash
git add js/luck.js tests/luck-games.test.js
git commit -m "feat: replace GIF/PNG dice engine with 3D GLB model-viewer roll-and-settle"
```

---

## Task 3: 기존 GIF/PNG 주사위 자산 제거

**Files:**
- Delete: `image/luck/dice_spin.gif`, `image/luck/dice_face_1.png` ~ `dice_face_6.png`

Task 2까지 마치면 이 7개 파일은 더 이상 코드에서 참조되지 않는다 (Task 15에서 임시로 만든 placeholder였다). 정적 텍스트 정규식 기반 테스트만 쓰는 이 저장소 특성상 파일 존재 여부가 테스트 결과에 영향을 주지 않으므로, 별도 red/green 사이클 없이 바로 삭제한다.

- [ ] **Step 1: 파일 삭제 확인**

```bash
grep -rn "dice_spin.gif\|dice_face_" js/ index.html
```

Expected: 아무 결과도 없음 (Task 2에서 이미 모든 참조를 교체했으므로)

- [ ] **Step 2: 삭제 및 커밋**

```bash
git rm image/luck/dice_spin.gif image/luck/dice_face_1.png image/luck/dice_face_2.png image/luck/dice_face_3.png image/luck/dice_face_4.png image/luck/dice_face_5.png image/luck/dice_face_6.png
git commit -m "chore: remove unused dice placeholder GIF/PNG assets (replaced by 3D GLB model)"
```

---

## Task 4: 전체 luck 테스트 스위트 재확인

**Files:** 없음 (검증 전용 task)

- [ ] **Step 1: 전체 luck 테스트 스위트 실행**

```bash
node tests/luck-migration.test.js && \
node tests/luck-supabase-client.test.js && \
node tests/luck-reward-integration.test.js && \
node tests/luck-screen-markup.test.js && \
node tests/luck-style-theme.test.js && \
node tests/luck-core.test.js && \
node tests/luck-games.test.js
```

Expected: 7개 모두 `passed` 메시지 출력, assertion 에러 없음

- [ ] **Step 2: JS 문법 검사**

```bash
node --check js/luck.js
```

Expected: 에러 없음 (조용히 종료)

---

## Task 5: (수동, 자동화된 red/green 사이클 없음) GLB 파일 배치 + 눈금-방향 보정

이 task는 사람이 직접 확인해야 하는 항목이다. 코드는 Task 1~4에서 이미 완성되며, 여기서는 실제 GLB 파일이 도착한 뒤 진행할 절차를 정리한다.

- [ ] **Step 1: GLB 파일 배치**

```bash
git add image/luck/dice_spin.glb
git commit -m "chore: add 3D dice GLB model asset"
```

- [ ] **Step 2: 보정용 임시 디버그 페이지 작성 (저장소에 커밋하지 않음)**

예:

```html
<!-- scratchpad/dice-calibration.html — 임시 파일, 커밋하지 않음 -->
<!DOCTYPE html>
<html>
<head>
  <script type="module" src="https://unpkg.com/@google/model-viewer/dist/model-viewer.min.js"></script>
</head>
<body>
  <model-viewer id="m" src="image/luck/dice_spin.glb" camera-controls style="width:400px;height:400px;"></model-viewer>
  <pre id="out"></pre>
  <script>
    const m = document.getElementById('m');
    setInterval(() => {
      document.getElementById('out').textContent = 'orientation: ' + (m.orientation || '(아직 값 없음, 카메라로 회전해보세요)');
    }, 300);
  </script>
</body>
</html>
```

브라우저로 열어 마우스로 모델을 돌려가며, 눈금 1~6이 각각 카메라를 정면으로 향할 때의 `orientation` 값을 육안으로 확인해 기록한다.

- [ ] **Step 3: 보정값을 `js/luck.js`에 반영**

`_DICE_FACE_ORIENTATION`의 6개 값을 Step 2에서 기록한 실제 값으로 교체한다:

```javascript
const _DICE_FACE_ORIENTATION = {
    1: '<실제 값>',
    2: '<실제 값>',
    3: '<실제 값>',
    4: '<실제 값>',
    5: '<실제 값>',
    6: '<실제 값>',
};
```

- [ ] **Step 4: 수동 QA**

1. luckScreen에서 주사위 게임 선택 → 모델이 계속 회전하는지 확인
2. "멈춤" 클릭 → 감속하며 정확한 눈금 방향으로 회전이 멈추는지 확인 (6번 정도 반복해 1~6 모두 확인)
3. 멈춘 뒤 약 0.8초간 결과가 눈에 보이게 정지해있다가 결과 화면(View6)으로 넘어가는지 확인
4. 결과 화면의 승/패가 실제로 보여진 눈금(6이면 승리)과 일치하는지 확인
5. 회전 중 "←" 뒤로가기 버튼으로 이탈 후 다른 게임(가위바위보/룰렛)을 선택했을 때 3D 모델이 남아있지 않고 정상적으로 이미지 게임이 보이는지 확인 (`_luckResetGameImg` 정리 확인)
6. 실기기(태블릿)에서 회전 애니메이션이 버벅이지 않는지 확인

- [ ] **Step 5: 커밋**

```bash
git add js/luck.js
git commit -m "chore: calibrate dice face orientations against the real GLB model"
```
